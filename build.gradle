buildscript {
    apply from: 'versions.gradle'
    addRepos(repositories)
    dependencies {
        classpath 'com.android.tools.build:gradle:3.1.1'
    }
}

allprojects {
    addResposAllProject(repositories)
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
