package com.example.gaute.codegeneration;

/**
 * Keep adding create table methods here, whenever you need
 *
 * @author gaute
 */
public class CreateAllEntities {
    public CreateAllEntities() {
//        createClientstatsCpusampleEntity();
    }

    /**
     * Each method should:
     * 1) create db (if not exist) and table (if not exists),
     * 2) define first fields as primary key in the table,
     * 3) should create (in the order) Entity class, Dao class, and Db class respectively
     *
     * Syntax of register method name:  register+ClientstatsCpusampleEntity
     */


    /**
     * creates a table from the fields and their type.
     * The first fields is always a primary key
     */
//    @CreateTable(fieldsClientstatsCpusampleEntity = {"timeStamp", "userTime", "systemTime", "cpuUsage"})
//    void createClientstatsCpusampleEntity(){
//        createTable(1L, 1L, 1L, 1L);
//    }
//
//    private void createTable(long l, long l1, long l2, long l3) {
//
//    }

}
